# ===========================================
# Hitster Game - Environment Variables
# ===========================================

# ----- Frontend (Vercel) -----

# PostgreSQL connection string (Neon)
DATABASE_URL="postgresql://user:password@host/database?sslmode=require"

# Socket server URL (Railway)
NEXT_PUBLIC_SOCKET_URL="https://your-socket-server.up.railway.app"


# ----- Socket Server (Railway) -----

# PostgreSQL connection string (same as above)
DATABASE_URL="postgresql://user:password@host/database?sslmode=require"

# CORS origin (your Vercel frontend URL)
CORS_ORIGIN="https://your-app.vercel.app"

# Socket server port (Railway sets this automatically via PORT)
SOCKET_PORT=3001
