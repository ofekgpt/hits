{
  "name": "hitster-socket",
  "version": "1.0.0",
  "main": "socket-server.ts",
  "scripts": {
    "start": "tsx socket-server.ts",
    "build": "npm install --prefix .. && npx prisma generate --schema=../prisma/schema.prisma"
  },
  "dependencies": {
    "@prisma/client": "^7.3.0",
    "dotenv": "^17.2.3",
    "prisma": "^7.3.0",
    "socket.io": "^4.8.3",
    "tsx": "^4.21.0"
  }
}
